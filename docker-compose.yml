services:
  api:
    build: .

    ports:
      - "8000:8000"

    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      OLLAMA_MODEL: qwen3:1.7b
      AI_TIMEOUT: 120

    restart: unless-stopped
